{% extends 'base.html.twig' %}

{% block title %}Série #{{ category.name }}
{% endblock %}

{% block body %}
	<h1>{{ category.name }}</h1>

    {% for program in programs %}
       <a href="{{ path("program_show", { id: program.id }) }}"><h1>{{ program.title }}</h1></a> 
    {% endfor %}
{% endblock %}
